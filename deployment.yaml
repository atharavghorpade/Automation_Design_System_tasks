---
apiVersion: v1
kind: Namespace
metadata:
  name: automation-system
  labels:
    name: automation-system

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: automation-config
  namespace: default
data:
  APP_ENV: "production"
  LOG_LEVEL: "INFO"

---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: default
type: Opaque
stringData:
  # Add your application secrets here
  database-url: ""
  api-key: ""

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: automation-design-system
  namespace: default
  labels:
    app: automation-design-system
    version: v1
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: automation-design-system
  template:
    metadata:
      labels:
        app: automation-design-system
        version: v1
    spec:
      imagePullSecrets:
        - name: ecr-registry-secret
      containers:
      - name: automation-app
        image: IMAGE_TAG  # This will be replaced by the CI/CD pipeline
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: APP_ENV
          valueFrom:
            configMapKeyRef:
              name: automation-config
              key: APP_ENV
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: automation-config
              key: LOG_LEVEL
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          exec:
            command:
            - pgrep
            - -f
            - java
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command:
            - pgrep
            - -f
            - java
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: input-data
          mountPath: /app/input
        - name: output-data
          mountPath: /app/output
      volumes:
      - name: input-data
        emptyDir: {}
      - name: output-data
        persistentVolumeClaim:
          claimName: automation-output-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: automation-design-system
  namespace: default
  labels:
    app: automation-design-system
spec:
  type: LoadBalancer
  selector:
    app: automation-design-system
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: automation-output-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: gp2  # AWS EBS storage class

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: automation-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: automation-design-system
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: automation-pdb
  namespace: default
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: automation-design-system
